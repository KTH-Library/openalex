# Lookup DOIs using OpenAlex

Lookup DOIs using OpenAlex

## Usage

``` r
openalex_doi_lookup(dois, resolution = c("all", "identifiers"))
```

## Arguments

- dois:

  a character vector of DOIs

- resolution:

  either "all" or "identifiers" to only return other related identifiers

## Value

tibble(s)
